// import { Test } from '../../src'

test('hello with correct name', () => {
  expect('Hello Jon').toBe('Hello Jon')
})

// ------ tests to do ------
// -- create botRouter with multiple bots
// -- getBot when bots is empty
// -- getBot when bots is empty
// -- getBotByUsername when bots is empty
// -- getBotByUsername when bots is not empty
